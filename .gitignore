node_modules/
dist/
.env
.env.local
*.log

# Local machine-specific tooling config (not shared across the team)
.claude/settings.local.json
.cursor/
.impeccable/
# Neon CLI: `.neon` pins this checkout to a project/branch, and `neon skills`
# vendors agent docs. Both are per-machine and regenerated by `neon link` /
# `neon skills`, so they stay out of the repo.
.neon
.agents/
skills-lock.json

# Isolated worktrees for subagent-driven development
.worktrees/

# Neon vendors agent skill docs here via `neon skills` (per-machine, regenerated).
.claude/skills/

# Hero source art. The served asset is the optimized heroimg.webp; this is
# the full-resolution export kept locally so it can be re-encoded later.
apps/web/public/heroimg.png
