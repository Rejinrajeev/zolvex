node_modules/
dist/
.env
.env.local
*.log

# Local machine-specific tooling config (not shared across the team)
.claude/settings.local.json
.cursor/
.impeccable/
# Neon CLI: `.neon` pins this checkout to a project/branch, and `neon skills`
# vendors agent docs. Both are per-machine and regenerated by `neon link` /
# `neon skills`, so they stay out of the repo.
.neon
.agents/
skills-lock.json

# Isolated worktrees for subagent-driven development
.worktrees/
